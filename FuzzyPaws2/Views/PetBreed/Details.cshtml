@model BreedDetailsViewModel

<br />
<div class="row">
    <div class="col-4">
        <img class="w-100" id="imgshadow"
            src="@FileLocation.RetriveFileFromFolder@Model.Image" class="img-thumbnail"  asp-append-version="true"
        alt="Card image cap">    
    </div>
                <div class="col">
                    <div>
                        <h5 class="card-title text-primary">@Model.Name</h5>
                         <hr />
                        <p class="card-text">@Model.Description</p>

                        <br /><br /><br />
                        <div class="btn btn-outline-info">
                            <a href="/Pet?breed=@Model.Name">
                                <h4 class="text text-primary">
                                Check out all our @(Model.Name.ToLower())s!
                                </h4>
                            </a>
                        </div>

                    </div>
                </div>  
                
 </div>

 <br />
 <hr />
@if (User.IsInRole("Admin"))
{
    <a asp-controller="PetBreed" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary mx-2">
        <i class="bi bi-pen"></i>
                        Edit
                    </a>
    <a asp-controller="PetBreed" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger"> 
                        <i class="bi bi-trash3"></i>
                        Delete
                    </a>
} &nbsp;
    <a asp-controller="PetBreed" asp-action="Index" class="btn btn-secondary">
    All breeds
    </a>

<style>

</style>                      